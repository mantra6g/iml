apiVersion: v1
data:
  cluster-pool-ipv4-cidr: 10.0.0.0/8
  cluster-pool-ipv4-mask-size: "24"
  cluster-pool-ipv6-cidr: fd00::/8
  cluster-pool-ipv6-mask-size: "64"
kind: ConfigMap
metadata:
  name: {{ include "loom-operator.resourceName" (dict "suffix" "iml-config" "context" $) }}
  namespace: {{ .Release.Namespace }}
