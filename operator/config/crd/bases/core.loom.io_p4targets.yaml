---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: p4targets.core.loom.io
spec:
  group: core.loom.io
  names:
    kind: P4Target
    listKind: P4TargetList
    plural: p4targets
    singular: p4target
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: P4Target is the Schema for the p4targets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of P4Target
            properties:
              driverIPs:
                description: DriverIPs are the in-cluster IPs assigned to the programmable
                  target's driver.
                items:
                  type: string
                type: array
              nfCIDR:
                description: NfCIDR is the range assigned to the network functions
                  running on this target.
                type: string
              taints:
                description: |-
                  Taints represents the taints applied to the P4 target, which can affect
                  scheduling and operation of network functions on it.
                items:
                  properties:
                    effect:
                      type: string
                    key:
                      type: string
                    timeAdded:
                      format: date-time
                      type: string
                    value:
                      type: string
                  required:
                  - effect
                  - key
                  type: object
                type: array
              targetIPs:
                description: TargetIPs are the IPs assigned to the programmable target.
                  They can be either in-cluster or public-facing IPs.
                items:
                  type: string
                type: array
              unschedulable:
                description: Unschedulable indicates whether the P4 target is unschedulable
                  for new network functions.
                type: boolean
            type: object
          status:
            description: status defines the observed state of P4Target
            properties:
              allocatable:
                additionalProperties:
                  anyOf:
                  - type: integer
                  - type: string
                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                  x-kubernetes-int-or-string: true
                description: Allocatable represents the resources of the P4 target
                  that are available for allocation
                type: object
              capacity:
                additionalProperties:
                  anyOf:
                  - type: integer
                  - type: string
                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                  x-kubernetes-int-or-string: true
                description: Capacity represents the total resources of the P4 target
                type: object
              conditions:
                items:
                  description: |-
                    P4TargetCondition represents the state of a specific aspect of the P4 target,
                    such as its readiness or health status.
                  properties:
                    lastHeartbeatTime:
                      format: date-time
                      type: string
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
