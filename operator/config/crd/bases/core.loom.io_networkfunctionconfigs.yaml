---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: networkfunctionconfigs.core.loom.io
spec:
  group: core.loom.io
  names:
    kind: NetworkFunctionConfig
    listKind: NetworkFunctionConfigList
    plural: networkfunctionconfigs
    singular: networkfunctionconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NetworkFunctionConfig is the Schema for the networkfunctionconfigs
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NetworkFunctionConfig
            properties:
              tables:
                additionalProperties:
                  properties:
                    defaultAction:
                      description: DefaultAction is the action to execute whenever
                        there is no match
                      properties:
                        name:
                          description: Name of the action to execute
                          type: string
                        parameters:
                          description: Parameters is a list of parameters for the
                            action.
                          items:
                            properties:
                              name:
                                description: Name of the field to match
                                type: string
                              type:
                                description: Type of the value
                                type: string
                              value:
                                description: Value to of the field to match against
                                type: string
                            required:
                            - name
                            - type
                            - value
                            type: object
                          type: array
                      required:
                      - name
                      type: object
                    entries:
                      description: Entries to be added to the table
                      items:
                        properties:
                          action:
                            description: Action is the action to take if the packet
                              matches the MatchFields.
                            properties:
                              name:
                                description: Name of the action to execute
                                type: string
                              parameters:
                                description: Parameters is a list of parameters for
                                  the action.
                                items:
                                  properties:
                                    name:
                                      description: Name of the field to match
                                      type: string
                                    type:
                                      description: Type of the value
                                      type: string
                                    value:
                                      description: Value to of the field to match
                                        against
                                      type: string
                                  required:
                                  - name
                                  - type
                                  - value
                                  type: object
                                type: array
                            required:
                            - name
                            type: object
                          matchFields:
                            description: MatchFields is a list of fields to match
                              against for this entry.
                            items:
                              properties:
                                name:
                                  description: Name of the field to match
                                  type: string
                                type:
                                  description: Type of the value
                                  type: string
                                value:
                                  description: Value to of the field to match against
                                  type: string
                              required:
                              - name
                              - type
                              - value
                              type: object
                            type: array
                        required:
                        - action
                        - matchFields
                        type: object
                      type: array
                  type: object
                description: Tables defines the configurations for each of the tables
                  in the network function
                type: object
            type: object
          status:
            description: status defines the observed state of NetworkFunctionConfig
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
