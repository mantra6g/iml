apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- iml_configmap.yaml

configMapGenerator:
- envs:
  - bmv2_config.env
  literals:
  - bmv2-control-plane-image=targets-bmv2:local
  - bmv2-data-plane-image=p4lang/behavioral-model:latest
  name: bmv2-config
  options:
    disableNameSuffixHash: true
