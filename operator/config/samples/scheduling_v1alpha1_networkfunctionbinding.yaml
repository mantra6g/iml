apiVersion: scheduling.loom.io/v1alpha1
kind: NetworkFunctionBinding
metadata:
  labels:
    app.kubernetes.io/name: loom-operator
    app.kubernetes.io/managed-by: kustomize
  name: networkfunctionbinding-sample
spec:
  # TODO(user): Add fields here
