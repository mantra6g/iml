apiVersion: infra.loom.io/v1alpha1
kind: LoomNode
metadata:
  labels:
    app.kubernetes.io/name: loom-operator
    app.kubernetes.io/managed-by: kustomize
  name: loomnode-sample
spec:
  # TODO(user): Add fields here
