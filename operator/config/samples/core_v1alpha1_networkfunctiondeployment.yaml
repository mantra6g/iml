apiVersion: core.loom.io/v1alpha1
kind: NetworkFunctionDeployment
metadata:
  labels:
    app.kubernetes.io/name: loom-operator
    app.kubernetes.io/managed-by: kustomize
  name: networkfunctiondeployment-sample
spec:
  # TODO(user): Add fields here
