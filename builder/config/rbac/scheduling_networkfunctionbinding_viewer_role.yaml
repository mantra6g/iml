# This rule is not used by the project builder itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to scheduling.desire6g.eu resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: builder
    app.kubernetes.io/managed-by: kustomize
  name: scheduling-networkfunctionbinding-viewer-role
rules:
- apiGroups:
  - scheduling.desire6g.eu
  resources:
  - networkfunctionbindings
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - scheduling.desire6g.eu
  resources:
  - networkfunctionbindings/status
  verbs:
  - get
