# This rule is not used by the project builder itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants full permissions ('*') over scheduling.desire6g.eu.
# This role is intended for users authorized to modify roles and bindings within the cluster,
# enabling them to delegate specific permissions to other users or groups as needed.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: builder
    app.kubernetes.io/managed-by: kustomize
  name: scheduling-networkfunctionbinding-admin-role
rules:
- apiGroups:
  - scheduling.desire6g.eu
  resources:
  - networkfunctionbindings
  verbs:
  - '*'
- apiGroups:
  - scheduling.desire6g.eu
  resources:
  - networkfunctionbindings/status
  verbs:
  - get
