---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: servicechains.cache.desire6g.eu
spec:
  group: cache.desire6g.eu
  names:
    kind: ServiceChain
    listKind: ServiceChainList
    plural: servicechains
    singular: servicechain
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServiceChain is the Schema for the servicechains API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ServiceChain
            properties:
              from:
                description: Specifies the source application
                properties:
                  name:
                    description: Name of the resource.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  namespace:
                    default: default
                    description: |-
                      Namespace of the resource.
                      Optional. Defaults to "default" if not set.
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - name
                type: object
              functions:
                description: Specifies the intermediate functions between the source
                  and destination applications
                items:
                  properties:
                    name:
                      description: Name of the resource.
                      maxLength: 253
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    namespace:
                      default: default
                      description: |-
                        Namespace of the resource.
                        Optional. Defaults to "default" if not set.
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                  required:
                  - name
                  type: object
                type: array
              to:
                description: Specifies the destination application
                properties:
                  name:
                    description: Name of the resource.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  namespace:
                    default: default
                    description: |-
                      Namespace of the resource.
                      Optional. Defaults to "default" if not set.
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - name
                type: object
            required:
            - from
            - to
            type: object
          status:
            description: status defines the observed state of ServiceChain
            properties:
              dst_app_uid:
                description: UID of the destinatination application.
                type: string
              nfs_uids:
                description: UID of the intermediate functions.
                items:
                  description: |-
                    UID is a type that holds unique ID values, including UUIDs.  Because we
                    don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                    intent and helps make sure that UIDs and names do not get conflated.
                  type: string
                type: array
              src_app_uid:
                description: UID of the source application.
                type: string
            required:
            - dst_app_uid
            - nfs_uids
            - src_app_uid
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
