---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: p4targets.core.desire6g.eu
spec:
  group: core.desire6g.eu
  names:
    kind: P4Target
    listKind: P4TargetList
    plural: p4targets
    singular: p4target
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: P4Target is the Schema for the p4targets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of P4Target
            properties:
              endpoint:
                description: Endpoint defines the connection details of the P4 target
                properties:
                  gnmiPort:
                    description: GNMIPort is the gNMI gRPC port of the P4 target
                    format: int32
                    type: integer
                  mgmtAddress:
                    description: ManagementAddress is the management IP address of
                      the P4 target
                    type: string
                  p4runtimePort:
                    description: P4RuntimePort is the P4Runtime gRPC port of the P4
                      target
                    format: int32
                    type: integer
                required:
                - gnmiPort
                - mgmtAddress
                - p4runtimePort
                type: object
              resources:
                description: Resources defines the compute resources allocated to
                  the P4 target
                properties:
                  cpu:
                    default: 1
                    description: CPU is the number of CPUs allocated to the P4 target
                    format: int32
                    minimum: 1
                    type: integer
                  memory:
                    default: 512
                    description: Memory is the amount of memory (in MiB) allocated
                      to the P4 target
                    format: int32
                    minimum: 128
                    type: integer
                type: object
              targetClass:
                description: TargetClass is the target class of the P4 target
                enum:
                - bmv2
                type: string
            required:
            - endpoint
            - targetClass
            type: object
          status:
            description: status defines the observed state of P4Target
            properties:
              allocatable:
                description: Allocatable is the amount of resources allocatable on
                  the P4 target
                properties:
                  cpus:
                    format: int32
                    type: integer
                  memory:
                    format: int32
                    type: integer
                type: object
              capacity:
                description: Capacity is the total amount of resources on the P4 target
                properties:
                  cpus:
                    format: int32
                    type: integer
                  memory:
                    format: int32
                    type: integer
                type: object
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastHeartbeatTime:
                description: LastHeartbeatTime is the last time the P4 target sent
                  a heartbeat
                format: date-time
                type: string
              phase:
                description: Phase is the current phase of the P4 target
                type: string
              used:
                description: Used is the amount of resources currently used on the
                  P4 target
                properties:
                  cpus:
                    format: int32
                    type: integer
                  memory:
                    format: int32
                    type: integer
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
