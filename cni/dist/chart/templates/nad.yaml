apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: {{ include "cni.fullname" . }}
  namespace: {{ include "cni.namespaceName" . }}
spec:
  config: |
    {
      "cniVersion": "{{ .Values.cniVersion }}",
      "type": "{{ .Values.type }}"
    }
