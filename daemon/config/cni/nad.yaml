apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: cni
spec:
  config: |
    {
      "cniVersion": "0.4.0",
      "type": "loom",
      "cni-args": {
        "app_type": "application"
      }
    }
